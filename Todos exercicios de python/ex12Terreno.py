##12. Calcular e exibir a area de um retângulo, a partir dos valores da base e altura que serao digitados. 
# Se a area for maior que 100, exibir a mensagem de Terreno grande.

base = float(input("Digite a base: "))
height = float(input("Digite a altura: "))

area = base * height;
print ("Area e ",area)
if (area>100):
    print("\n O terreno e grande")



